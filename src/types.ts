export type TUsers = {
  id: string;
  name: string;
  email: string;
  password: string | number;
};

export type TProducts = {
  id: string;
  name: string;
  price: number;
  description: string;
  imageUrl: string;
};
